name: Build Status Check

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - name: Deployment Status
        run: echo "✅ Repository updated. Netlify auto-deploying to https://www.amruthasagar.com"
